{{# Card cardClassName='card-profile-edit'}}
  {{{ Avatar userInitials='ИИ' avatarType='card-profile' }}}
  {{# Title level='3'}}
    Иван
  {{/Title}}
  {{# Form}}
    {{#each profileEditPassword}}
      {{{ ProfileEditItem 
        label=this.label 
        htmlType=this.htmlType 
        name=this.name
        id=this.id
      }}}
    {{/each}}
    <div class="card-profile__controls">
      {{# Button type='primary' page="profilePage"}}
        Сохранить
      {{/Button}}
    </div>  
  {{/Form}}
{{/Card}}
