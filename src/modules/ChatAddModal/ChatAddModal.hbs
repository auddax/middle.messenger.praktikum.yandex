{{# Backdrop}}
  {{# Modal}}
    {{# Form formId='chatNameForm' formClassName='modal__form-controls'}}
      {{{ Input 
        label='Название чата' 
        className='modal__input' 
        name='chat_name' 
        id='chatNameInput' 
        onFocusOut=this.focusOutHandler 
      }}}
      {{# Button type='primary' onClick=this.handleCreateChat }}
        Создать
      {{/Button}}
    {{/Form}}
  {{/Modal}}
{{/Backdrop}}